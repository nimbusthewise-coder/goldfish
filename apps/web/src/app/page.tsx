/**
 * Home Page
 * 
 * Default view uses ConstellationView from AppShell.
 * This component is now optional since AppShell provides the default layout.
 */

export default function Home() {
  // AppShell provides the ConstellationView by default
  // This component can be used for route-specific content in the future
  return null;
}
